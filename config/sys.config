[
  {gp_api, [
    {port, 8080},
    {listen_addr, "0.0.0.0"}
  ]},
  {gp_store_client, [
    {socket_path, "/tmp/gp_store.sock"},
    {pool_size, 8}
  ]},
  {gp_delivery, [
    {poll_interval_ms, 1000},
    {workers, 16},
    {claim_count, 20},
    {lease_secs, 30}
  ]},
  {kernel, [
    {logger_level, info},
    {logger, [
      {handler, default, logger_std_h, #{
        config => #{type => standard_io},
        formatter => {logger_formatter, #{
          template => [time, " [", level, "] ", msg, "\n"]
        }}
      }}
    ]}
  ]},
  {prometheus, [
    {collectors, [
      prometheus_boolean,
      prometheus_counter,
      prometheus_gauge,
      prometheus_histogram,
      prometheus_summary
    ]}
  ]}
].
